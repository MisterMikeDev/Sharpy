import { CommandInteraction, CacheType, User } from "discord.js";
import { Sharpy } from "../../Client";

export const DefinitiveBanApealCommand = async ({
    Sharpy,
    interaction,
    user
}: {
    Sharpy: Sharpy;
    interaction: CommandInteraction<CacheType>;
    user: User;
}) => {};
